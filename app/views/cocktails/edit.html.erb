<%=link_to "back to the cocktail", cocktail_path(@cocktail), class: "btn btn-primary" %>

<%= simple_form_for [@cocktail] do |f| %>
  <%= f.input :name %>
  <%= f.submit "edit the cocktail", class: "btn btn-primary" %>
<% end %>

